import { RevisionPipePipe } from './revision-pipe.pipe';

describe('RevisionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RevisionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
